const colors = {
  bg_light_blue: '#a2c5fc',
  bg_mild_blue: '#5898fc',
  navy: '#0449b5',
  line_default: '#dae1eb',
  white: '#FFFFFF',
  black: '#111112',
}

const theme = {
  colors,
}

export type CustomThemeType = typeof theme

export default theme
